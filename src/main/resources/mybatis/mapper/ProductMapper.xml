<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cosmetic.shop.product.ProductMapper">

<select id="getProductListBysecondCategory" resultType="com.cosmetic.shop.admin.product.ProductVO">
<!-- 주입과 상관없이 결과를 com.cosmetic.shop.admin.product.ProductVO -->	

		select 
			pro_num,cate_code,pro_name,pro_price,pro_discount,pro_publisher,pro_content,pro_up_folder,pro_img,pro_amount,pro_buy, pro_review, pro_date,pro_updatedate
		from 
			product_tbl
		where
			cate_code = #{cate_code}
		and
			pro_buy = 'Y'
							
		limit #{cri.pageStart}, #{cri.perPageNum}
	
	</select>
	
	<select id="getCountProductListBysecondCategory" resultType="int">
	
		select
			count(*)
		from 
			product_tbl
		where 
			cate_code = #{cate_code}
		and
			pro_buy = 'Y'
	
	</select>















</mapper>